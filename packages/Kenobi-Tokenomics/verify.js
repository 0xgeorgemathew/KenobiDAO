const hre = require("hardhat");
const fs = require("fs");
const uniswapSdk = require("@uniswap/v3-sdk");
async function main() {}
main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
